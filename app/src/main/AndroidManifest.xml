<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Expedited/foreground episode downloads (WorkManager) on Android 14+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required by LoudnessEnhancer (the "Boost volume" audio effect). -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:name=".OndesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ondes"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <!-- Declares Android Auto support (browsable media app). -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Ondes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Marks this activity as *the* media app to open. Several
                     surfaces that show playback outside the app - the lock
                     screen, OEM "island" / floating media capsules, car head
                     units, the assistant - resolve "open the player" with
                     ACTION_MAIN + CATEGORY_APP_MUSIC rather than through the
                     session activity. Without the category Ondes is not a
                     candidate at all and the system silently falls back to
                     whatever music app the user has, so the capsule for an
                     Ondes episode could open a different player entirely. -->
                <category android:name="android.intent.category.APP_MUSIC" />
            </intent-filter>
        </activity>

        <service
            android:name=".playback.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <!-- Lets Android Auto / Wear OS discover & browse this app. -->
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Keeps Ondes as the owner of the media buttons once its session is
             gone. Media3 only registers a *runtime* receiver when the manifest
             declares none, and that receiver dies with the process: from API 31
             on, the platform session is then left without a media-button
             broadcast receiver, so a play/open from a headset, a car, the lock
             screen or a resumption capsule after the process was killed is
             routed to whichever other media app still declares one. Declaring
             the receiver also makes the MediaLibrarySession.Callback's
             onPlaybackResumption reachable after process death, which is what
             it was written for. -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- WorkManager runs expedited episode downloads as a foreground service on
             older API levels; declare the data-sync type it posts with. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
